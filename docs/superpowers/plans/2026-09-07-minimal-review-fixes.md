# 第二轮审查的最小修复执行计划

基线：`e50deb2`。依据：`artifacts/review-2026-09-07/review.md` 及其 parser、CLI、宿主模拟和浏览器证据。沿用现有主题、框架和生成模型，按行为边界分批提交。

| 批次 | 问题 | 最小改动 | 验收 |
|---|---|---|---|
| A | R01 配置丢失 | CLI 与扩展使用已有 YAML 解析器按映射更新配置；非法/重复键停止写入；保留未知字段与旧模式兼容语义 | 注释、行内映射、嵌套未知配置、重复键；CLI 实际文件与扩展行为测试 |
| B | R02/R03/R06 内容与输入 | Builder 整页操作明确为追加到文稿末尾；空文件生成合法首页；副标题仅同步未改写的封面正文；页面标题单一编辑入口，保留隐藏标题语义 | 原 headmatter 与全部原页面内容不变；自定义封面正文保留；标题生成一致；宿主和真实 Webview 回归 |
| C | R04/R05 主题 UI | 修正七处 frontmatter 读取；恢复页眉后的内容留白；移除覆盖 MetricGrid 显式列数的窗口断点 | 六种布局标题/副标题、局部章节配色；窄宽窗口列数一致；内容不被页眉遮挡，代表性播放/导出 |
| D | R07 引用交付 | 生成带引用的工作流后明确提示 bibliography 依赖，并提供现有 CLI 的完整模板初始化入口；保留当前 Markdown | 六个引用模板出现正确提示/入口，无引用模板不打扰；取消无副作用；不覆盖已有 bibliography |
| E | 集成 | 同步必要文档与受影响预览；更新执行记录 | 常规总检查、插件 lint/独立打包与针对性视觉检查；最终提交后远程 CI |

顺序：A → B → C → D → E。每批记录实际通过/失败结果后提交。未证实的表格裁切、打印字体和 Windows shell 问题不并入本次修复；原有实际读屏输出验收仍单独保留，不宣称完成。

R01 的 YAML 序列化可能规范化缩进和注释；配置值和未知字段必须完整保留。CLI 使用直接声明的已有解析器版本，扩展复用包内 standalone YAML 运行时。R02 将按钮、说明和宿主行为一起改为“追加”，避免保留暗示任意光标插入的文案。

## 执行记录

- A 已完成：CLI 与扩展按 YAML 映射更新配置，删除旧写入正则；保留嵌套字段和各自的旧模式兼容逻辑。19 项针对性测试通过，新增负例在修复前均以重复键失败，修复后通过。日志：`artifacts/fixes-2026-09-07/a-before.log`、`a-after.log`、`a-compile.log`。
- 根包补充 `js-yaml: 4.2.0` 直接依赖和锁文件 importer；版本与完整性记录已存在于锁文件。离线元数据不完整，未升级其他依赖；全新安装由最终远程 CI 验证。
- B 已完成：页面追加至文件末尾，空 Markdown 创建合法主题首页；副标题保留自定义封面正文；标题使用单一文本入口，目录标题保留显示/隐藏选择。34 项插件测试、lint 和中英文/明暗/五视口 Webview 回归通过。真实 parser 验证原有全局配置与每页内容保持一致。证据：`b-host-before.log`、`b-tests.log`、`b-ui.log`、`b-lint.log`。
- B 空白边界补充：仅含空格、换行或制表符的 Markdown 按空文件处理，替换空白后生成首页；有内容的文稿继续追加。新增实际 parser 负例先得到两页，修复后得到一页且保留全局主题；34 项插件测试和 lint 再次通过。证据：`b-whitespace-before.log`、`b-whitespace-tests.log`、`b-whitespace-lint.log`。最终 VSIX 重新构建并复核，见 `e-vsix-package-final.log`、`e-vsix-check-final.log`。
- C 已完成：七处 frontmatter 使用 Slidev 提供的普通对象；六种页眉参与自然排版，正文按实际高度避让；MetricGrid 遵守显式列数。新增浏览器检查先复现缺失页眉，修复后通过六种布局的标题、副标题、长标题避让、局部章节浅色及两种窗口的单列断言；七页 PDF 导出成功。证据：`c-before.log`、`c-after.log`、`c-after/`。章节截图改用视口截图，避免元素稳定性等待超时；已人工检查浅色章节与窄窗指标截图。
- D 已完成：六个引用工作流在生成 Markdown 后提示 `references.bib` 依赖，可调用已有完整模板初始化命令；普通工作流无提示，关闭通知不触发创建。先复现缺失提示，再通过 34 项插件测试、lint 和模板检查；实际 CLI 验证拒绝覆盖非空项目并保留用户 bibliography。中英文通知与使用说明已同步。证据：`d-before.log`、`d-tests.log`、`d-lint.log`、`d-templates.log`。
- C 导出复查补充：恢复页眉后，议程和致谢还会显示内部重复标题；七项议程因此越过内容顶部。内部标题改为仅在无页眉时显示；实际七项议程的全部内容边界、两种布局的重复标题检查及七页 PDF 导出通过。证据：`c-final.log`、`c-final/`。检查切换 16:9 与 4:3 夹具时明确重启 Slidev 服务，避免依赖开发服务器配置热更新时序。
- E 本地集成已完成：常规总检查通过，含 66 项根项目测试、34 项扩展测试、文档构建及 54 组合的配置 dry-run。34 张布局图重新导出，仅六张受影响布局图发生变化；Builder 按钮截图及扩展 91 项预览清单同步。VSIX 重新打包并在仓库外验证全部 8 个模板、91 个图片哈希、YAML 运行时和许可证。证据：`e-check-final.log`、`e-layout-previews-final.log`、`e-vsix-package.log`、`e-vsix-check.log`。
- 依赖复核：本机 pnpm 12 的自动重装被沙箱网络限制中断；使用 pnpm 10.30.3 冻结安装成功，并固定真实 Node/pnpm 路径运行全部常规检查。未修改用户的全局工具配置。证据：`e-install-pnpm10.log`。
- UI 机械扫描得到 5 项原有风格提示，涉及方法图的网格背景与引用/强调块的侧边标记，均未触及本次新增排版规则；Builder 没有扫描命中。未为消除风格提示而改造既有学术主题。证据：`e-ui-detector.json`。

本次 R01–R07 的最小修复均已实现。完整 54 组合视觉导出未在本轮重复执行，采用上述针对性浏览器与实际布局导出证据；实际读屏输出仍保留为原计划的独立验收项。远程 CI 以最终分支提交对应的 GitHub Actions 运行记录及任务回复为准。
